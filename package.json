{
  "name": "loopira",
  "private": true,
  "description": "Release tooling for Loopira (semantic-release). Application code lives in /internal and /web.",
  "devDependencies": {
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/git": "^10.0.1",
    "conventional-changelog-conventionalcommits": "^8.0.0",
    "semantic-release": "^24.2.3"
  }
}
